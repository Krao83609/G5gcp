name: Test Schedule

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  hello_world:
    runs-on: ubuntu-latest

    steps:
      - name: Say hello
        run: echo "Current server time: $(date)"

      - name: Every 5 mins
        run: echo "This workflow runs every 5 minutes"
